# G1 probe run — live x402 sellers and KeeperHub

Date: 2026-09-09
Command: `pnpm probe:all`  ·  Exit: 0  ·  G1: PASSED

Targets: one resource per distinct host from the live x402 discovery index
(46 hosts). The index URL is configuration, not a compiled-in fact (§17); it was
supplied in PROBE_X402_TARGETS.

```
G1 — no compiled-in protocol facts, and probes read live terms (§17, §22)

[1/3] address-literal check over apps/ and packages/
      clean: 55 files, no compiled-in protocol fact

[2/3] x402 — reading advertised terms from live sellers
  READ  https://api.onesource.io/api/chain/erc20-balance
        x402 v2, terms in the header, 2 offer(s) published, 2 parsed
        scheme=exact network=eip155:8453 amount=3000 (atomic)
        maxTimeoutSeconds=3600  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  FAIL  https://stableenrich.dev/api/fullenrich/people-search
        NO_PAYMENT_REQUIRED: expected HTTP 402, received 405
  FAIL  https://api.exa.ai/search
        NO_PAYMENT_REQUIRED: expected HTTP 402, received 404
  READ  https://kronossignals.com/api/v1/liquidations/btc
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=20000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://x402.ottoai.services/crypto-news
        x402 v2, terms in the header, 3 offer(s) published, 3 parsed
        scheme=exact network=eip155:8453 amount=1000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  FAIL  https://stableupload.dev/api/upload
        NO_PAYMENT_REQUIRED: expected HTTP 402, received 405
  READ  https://crypto.apitoll.cloud/v1/crypto/price
        x402 v2, terms in the header, 2 offer(s) published, 2 parsed
        scheme=exact network=eip155:8453 amount=1000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://api.bitrefill.com/x402/gift-cards/search
        x402 v2, terms in the header, 4 offer(s) published, 4 parsed
        scheme=exact network=eip155:8453 amount=2000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://agi.apify.com/protocols/x402/prepaid-tokens
        x402 v2, terms in the header, 3 offer(s) published, 3 parsed
        scheme=exact network=eip155:8453 amount=1000000 (atomic)
        maxTimeoutSeconds=60  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
  READ  https://laso.finance/get-card
        x402 v2, terms in the header, 2 offer(s) published, 2 parsed
        scheme=exact network=eip155:8453 amount=5000000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://cheaptokens.ai/api/buy
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=1000000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://vibesprings.net/api/price/btc-usd
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=2000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://x402lifeadvice.vercel.app/api/life-advice
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=1000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://coinflip402.vercel.app/api/coinflip
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=1000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://x402uselessfacts.vercel.app/api/useless-fact
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=1000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://memegeneratorx402.vercel.app/api/generate-meme
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=1000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://ladyfortunalx402.vercel.app/api/fortune
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=1000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  FAIL  https://glim.sh/api/v1/twitter/search
        NO_PAYMENT_REQUIRED: expected HTTP 402, received 404
  READ  https://dicex402.vercel.app/api/dice
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=1000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://riddlex402.vercel.app/api/riddle
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=2000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://roastx402endpoint.vercel.app/api/roast
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=1700 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://randomfactsx402.vercel.app/api/random-fact
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=1000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://horrorstoryapi.vercel.app/api/horror-story
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=1300 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://jokes-endpoint.vercel.app/api/jokes
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=2000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://402timezones.vercel.app/api/convert-timezone
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=1000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://x402wordoftheday.vercel.app/api/word-of-the-day
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=1500 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://cars2026.vercel.app/api/cars
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=1100 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://weight402endpoint.vercel.app/api/convert-weight
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=2500 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://api.loyalspark.online/x402-gateway/recipient-api/offers
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=1000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://hypernatt.com/api/m2m/liq-radar
        x402 v2, terms in the header, 2 offer(s) published, 2 parsed
        scheme=exact network=eip155:8453 amount=1000 (atomic)
        maxTimeoutSeconds=60  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://api.kadec0.xyz/v1/serp
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=50000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://flights.use.x402atlas.com/search/:departure/:arrival
        x402 v2, terms in the header, 3 offer(s) published, 3 parsed
        scheme=exact network=eip155:8453 amount=50000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://api-dev-v2.ixs.finance/x402/vaults
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=1000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  FAIL  https://x402.tavily.com/search
        NO_PAYMENT_REQUIRED: expected HTTP 402, received 404
  READ  https://edgar.apitoll.cloud/v1/edgar/filings
        x402 v2, terms in the header, 2 offer(s) published, 2 parsed
        scheme=exact network=eip155:8453 amount=3000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://rubric-protocol.com/v1/x402/hedera-facts/supply
        x402 v2, terms in the header, 3 offer(s) published, 3 parsed
        scheme=exact network=eip155:8453 amount=1000 (atomic)
        maxTimeoutSeconds=120  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
  FAIL  https://stablestudio.dev/api/upload
        NO_PAYMENT_REQUIRED: expected HTTP 402, received 405
  FAIL  https://x402.miroshark.xyz/run
        NO_PAYMENT_REQUIRED: expected HTTP 402, received 405
  READ  https://google-trends.use.x402atlas.com/trend
        x402 v2, terms in the header, 4 offer(s) published, 4 parsed
        scheme=exact network=eip155:8453 amount=50000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  FAIL  https://vaaya.ai/api/run/exa/search
        NO_PAYMENT_REQUIRED: expected HTTP 402, received 200
  READ  https://api.hyperextend.xyz/v1/liquidations/BTC
        x402 v2, terms in the header, 2 offer(s) published, 1 parsed
        DRIFT accepts[1]: amount: atomic amount must be integral  <- offer excluded, never guessed at
        scheme=exact network=eip155:8453 amount=111000 (atomic)
        maxTimeoutSeconds=60  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://gas.apitoll.cloud/v1/base/gas
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=1000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://blockrun.ai/api/v1/exa/search
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=11000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
  READ  https://quartermaster.surewhynot.app/v1/gas
        x402 v2, terms in the header, 2 offer(s) published, 2 parsed
        scheme=exact network=eip155:8453 amount=1000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://stabletravel.dev/api/seats-aero/search
        x402 v2, terms in the header, 2 offer(s) published, 2 parsed
        scheme=exact network=eip155:8453 amount=20000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)
  READ  https://chat.gedx402.com/v1/chat/completions
        x402 v2, terms in the header, 1 offer(s) published, 1 parsed
        scheme=exact network=eip155:8453 amount=1000 (atomic)
        maxTimeoutSeconds=300  <- payment window, NOT a response deadline (D-002)
        checks available: mimeType=yes schema=NO latencySLA=NO (x402 advertises none)
        bazaar info.output.type=json (reported only, never a verdict input — D-005)

[3/3] KeeperHub — reading the chain list from the live API
  READ  KeeperHub (https://app.keeperhub.com)
        24 chains read from the live API, none compiled in
        credential: not supplied or not checked

service state: GATING

G1: PASSED
```
